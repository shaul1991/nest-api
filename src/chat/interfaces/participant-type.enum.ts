export enum ParticipantType {
  USER = 'user',
  GUEST = 'guest',
}

export enum RoomType {
  PUBLIC = 'public',
  PRIVATE = 'private',
}

export enum MessageType {
  TEXT = 'text',
  SYSTEM = 'system',
}
