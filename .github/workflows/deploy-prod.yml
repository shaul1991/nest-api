name: Deploy Production

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "deploy" to confirm production deployment'
        required: true
        default: ''

env:
  NODE_VERSION: '22'

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.event.inputs.confirm == 'deploy'
    steps:
      - name: Notify Slack - Deploy Started
        run: |
          curl -X POST ${{ secrets.SLACK_WEBHOOK_URL }} \
            -H "Content-Type: application/json" \
            -d '{
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "🔄 Production 배포 시작",
                    "emoji": true
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*Triggered by:*\n${{ github.actor }}"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Branch:*\nrelease"
                    }
                  ]
                }
              ]
            }'

      - name: Trigger Jenkins Deployment
        run: |
          echo "Triggering Jenkins job nest-api-prod"

          # Try without CSRF first (if CSRF is disabled in Jenkins)
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" -X POST \
            -u "${{ secrets.JENKINS_USER }}:${{ secrets.JENKINS_API_TOKEN }}" \
            "${{ secrets.JENKINS_URL }}/job/홈%20서버/job/nest-api-prod/build")

          if [ "$HTTP_CODE" = "201" ] || [ "$HTTP_CODE" = "200" ]; then
            echo "Build triggered successfully (HTTP $HTTP_CODE)"
            exit 0
          fi

          # If 403, try with CSRF crumb
          if [ "$HTTP_CODE" = "403" ]; then
            echo "Got 403, trying with CSRF crumb..."

            CRUMB_RESPONSE=$(curl -s -u "${{ secrets.JENKINS_USER }}:${{ secrets.JENKINS_API_TOKEN }}" \
              "${{ secrets.JENKINS_URL }}/crumbIssuer/api/json")

            CRUMB=$(echo "$CRUMB_RESPONSE" | jq -r '.crumb // empty')

            if [ -n "$CRUMB" ]; then
              curl -X POST \
                -u "${{ secrets.JENKINS_USER }}:${{ secrets.JENKINS_API_TOKEN }}" \
                -H "Jenkins-Crumb: $CRUMB" \
                "${{ secrets.JENKINS_URL }}/job/홈%20서버/job/nest-api-prod/build" \
                --fail --show-error
              echo "Build triggered successfully with CSRF crumb"
            else
              echo "Failed to get CSRF crumb. Response: $CRUMB_RESPONSE"
              exit 1
            fi
          else
            echo "Unexpected HTTP code: $HTTP_CODE"
            exit 1
          fi

      - name: Notify on Failure
        if: failure()
        run: |
          curl -X POST ${{ secrets.SLACK_WEBHOOK_URL }} \
            -H "Content-Type: application/json" \
            -d '{
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "❌ Production 배포 트리거 실패",
                    "emoji": true
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "Jenkins 연결에 실패했습니다. 수동으로 배포해주세요."
                  }
                }
              ]
            }'
